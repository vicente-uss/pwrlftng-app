# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

For every UI or UX task, read `DESIGN.md` completely before editing. Treat it as
the repository's visual and interaction contract unless the user provides a more
specific approved reference for the current task.
